import type { ScoreDocument } from "./schema";

export async function importMusicXmlToScore(_xml: string): Promise<ScoreDocument> {
  throw new Error("Not implemented. Build in Phase 1 after Phase 0 gate passes.");
}